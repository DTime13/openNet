# OpenNet
```
抱歉, 目前沒有私人電腦，所以只是簡單安裝 sublime 就開搞了
其他東西都沒安裝也就不會有執行畫面，非常抱歉。跑不跑得動還是個謎。
基本上, 這份程式碼是基於 robot framework 的實作目標，詢問 AI 在 pytest時該怎麼做的結果
但還無法讓 tests 看起來像自然語言，另外如果架構上有斷手斷腳的話還請多多見諒。
但凡不嫌棄的話或許從 tests -> behaviors -> pages 這樣看下來應該會對眼睛比較友善
但如果不想看也是可以理解的，以上。報告完畢。
```

## Requirements
```
python 3.12+
```

## Installation
```bash
pip install -r requirements.txt
```
